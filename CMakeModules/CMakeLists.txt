set( src )
list( APPEND src FindCheckMATE.cmake )
list( APPEND src FindEigen3.cmake )
list( APPEND src FindFEYNHIGGS.cmake )
list( APPEND src FindFORTRANFLUSH.cmake )
list( APPEND src FindGFORTRANLIB.cmake )
list( APPEND src FindGM2Calc.cmake )
list( APPEND src FindHepMC.cmake )
list( APPEND src FindHIGGSBOUNDS.cmake )
list( APPEND src FindHIGGSSIGNALS.cmake )
list( APPEND src FindIFORTLIB.cmake )
list( APPEND src FindLHAPDF.cmake )
list( APPEND src FindMadGraph5.cmake )
list( APPEND src FindMICROMEGAS.cmake )
list( APPEND src FindROOT.cmake )
list( APPEND src FindSLHAEA.cmake )
list( APPEND src FindSModelS.cmake )
list( APPEND src FindSPheno.cmake )
list( APPEND src FindSUPERISO.cmake )
list( APPEND src FindVecCore.cmake )


add_custom_target( CMakeModules SOURCES ${src} )
