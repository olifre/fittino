#include_directories(./include
#                    ../exceptions/include)
include_directories(./include)
#aux_source_directory(src sources)
#add_definitions(`root-config --cflags`)
#link_libraries(ROOT `root-config --libs`)
#add_library(interpreters ${sources})
